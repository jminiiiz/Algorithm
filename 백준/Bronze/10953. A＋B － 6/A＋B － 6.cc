#include <bits/stdc++.h>
using namespace std;
int a, b, t;
char c;
int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> t;
    while (t--) {
       cin >> a >> c >> b;
       cout << a + b << "\n";
    }
}